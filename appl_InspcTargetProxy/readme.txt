The InspcTargetProxy is a tool which reads a Reflection.bin file which is associated to data structures of any target.
The target 
* The include/InspcTargetProxyTelg.h contains the definition of stuctures which should be used in any target.